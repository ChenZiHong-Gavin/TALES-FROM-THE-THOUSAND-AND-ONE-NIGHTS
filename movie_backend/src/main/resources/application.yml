spring:
  data:
#    mongodb:
#      host: localhost
#      port: 27017
#      database: movie
     mongodb:
      host: localhost
      port: 27017
      authentication-database: admin
      database: movie
      username: root
      password: '***REMOVED***'
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
      enabled: true

server:
  port: 8080
  servlet:
    context-path: /api
